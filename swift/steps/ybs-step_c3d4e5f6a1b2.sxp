(doc
  (title "Step 3: Swift Packet Structure")
  (source-format markdown)
  (converted "2026-07-19")
  (kv "Version" "0.1.0")
  (section
    (heading "Overview")
    (para "Implement Packet struct in Swift with wire-format compatibility."))
  (section
    (heading "What This Step Builds")
    (para "Implements `Packet` struct — the YX wire-format data structure with `hmac: Data`, `guid: Data`, and `payload: Data` fields plus `Data` serialization/deserialization."))
  (section
    (heading "Step Objectives")
    (item "1. Packet struct (hmac, guid, payload)")
    (item "2. Serialization/deserialization")
    (item "3. XCTest unit tests")
    (item "4. 100% coverage"))
  (section
    (heading "Prerequisites")
    (item "Step 2 completed"))
  (section
    (heading "Traceability")
    (kv "Implements" "protocol/specs/technical/yx-protocol-spec.sxp § Wire Format"))
  (section
    (heading "Instructions")
    (subsection
      (heading "Before Starting — Record Start Time")
      (para "Record the current timestamp in ISO 8601 format: `YYYY-MM-DDTHH:MM:SSZ` This is used for duration calculation in the DONE file."))
    (subsection
      (heading "1. Create Packet")
      (para "Create `Sources/YXProtocol/Transport/Packet.swift`:")
      (code "import Foundation

public struct Packet {
    public let hmac: Data      // 16 bytes
    public let guid: Data      // 6 bytes
    public let payload: Data   // Variable

    public init(hmac: Data, guid: Data, payload: Data) throws {
        guard hmac.count == 16 else {
            throw PacketError.invalidHMACLength
        }
        guard guid.count == 6 else {
            throw PacketError.invalidGUIDLength
        }

        self.hmac = hmac
        self.guid = guid
        self.payload = payload
    }

    public func toBytes() -> Data {
        return hmac + guid + payload
    }

    public static func fromBytes(_ data: Data) -> Packet? {
        guard data.count >= 22 else { return nil }

        let hmac = data[0..<16]
        let guid = data[16..<22]
        let payload = data[22...]

        return try? Packet(hmac: Data(hmac), guid: Data(guid), payload: Data(payload))
    }
}

public enum PacketError: Error {
    case invalidHMACLength
    case invalidGUIDLength
}"))
    (subsection
      (heading "2. Create Tests")
      (para "Create `Tests/YXProtocolTests/Unit/PacketTests.swift`:")
      (code "import XCTest
@testable import YXProtocol

final class PacketTests: XCTestCase {
    func testCreateValidPacket() throws {
        let packet = try Packet(
            hmac: Data(repeating: 0x00, count: 16),
            guid: Data(repeating: 0x01, count: 6),
            payload: Data(\"test\".utf8)
        )

        XCTAssertEqual(packet.hmac.count, 16)
        XCTAssertEqual(packet.guid.count, 6)
        XCTAssertEqual(packet.payload, Data(\"test\".utf8))
    }

    func testToBytes() throws {
        let packet = try Packet(
            hmac: Data(repeating: 0xaa, count: 16),
            guid: Data(repeating: 0xbb, count: 6),
            payload: Data(\"test\".utf8)
        )

        let data = packet.toBytes()
        XCTAssertEqual(data.count, 26)
        XCTAssertEqual(data[0..<16], Data(repeating: 0xaa, count: 16))
        XCTAssertEqual(data[16..<22], Data(repeating: 0xbb, count: 6))
    }

    func testFromBytes() {
        let data = Data(repeating: 0xaa, count: 16) +
                   Data(repeating: 0xbb, count: 6) +
                   Data(\"test\".utf8)

        let packet = Packet.fromBytes(data)
        XCTAssertNotNil(packet)
        XCTAssertEqual(packet?.payload, Data(\"test\".utf8))
    }

    func testRoundtrip() throws {
        let original = try Packet(
            hmac: Data(repeating: 0x01, count: 16),
            guid: Data(repeating: 0x02, count: 6),
            payload: Data(\"payload\".utf8)
        )

        let data = original.toBytes()
        let restored = Packet.fromBytes(data)

        XCTAssertNotNil(restored)
        XCTAssertEqual(restored?.hmac, original.hmac)
        XCTAssertEqual(restored?.guid, original.guid)
        XCTAssertEqual(restored?.payload, original.payload)
    }
}"))
    (subsection
      (heading "3. Run Tests")
      (code "swift test --filter PacketTests")))
  (section
    (heading "Verification")
    (item "[ ] Tests pass")
    (item "[ ] Packet creation works")
    (item "[ ] Serialization works")
    (item "[ ] Roundtrip works")
    (code "swift test --filter PacketTests"))
  (section
    (heading "Documentation")
    (para "**Record end time** and calculate duration (end − start timestamp).")
    (para "Create `docs/build-history/ybs-step_c3d4e5f6a1b2-DONE.txt`:")
    (code "STEP ybs-step_c3d4e5f6a1b2: Swift Packet Structure
STARTED:    [start timestamp from Before Starting]
COMPLETED:  [ISO 8601 timestamp]
DURATION:   [minutes]

OBJECTIVES COMPLETED:
[copy from Step Objectives above]

FILES CREATED/MODIFIED:
- Sources/YXProtocol/Transport/Packet.swift
- Tests/YXProtocolTests/Unit/PacketTests.swift

VERIFICATION: PASSED (attempt [N])

NEXT STEP: ybs-step_d4e5f6a1b2c3 (HMAC Computation (CryptoKit))")
    (para "Update `BUILD_STATUS.sxp`: mark this step `[x]` and update **Last Updated** timestamp."))
  (section
    (heading "Success Criteria")
    (para "This step is successful when:")
    (item "1. All verification checks pass (within 3 attempts)")
    (item "2. All required files exist and are valid")
    (item "3. Build compiles/runs without errors")
    (item "4. DONE file created in `docs/build-history/`")
    (item "5. `BUILD_STATUS.sxp` updated"))
  (section
    (heading "Next Steps")
    (para "After completing this step, proceed to:")
    (kv "Next" "`ybs-step_d4e5f6a1b2c3` — HMAC Computation (CryptoKit)"))
  (section
    (heading "Version History")
    (subsection
      (heading "0.1.0 (2026-04-11)")
      (item "Initial step creation"))))
